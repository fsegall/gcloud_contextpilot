# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/
.coverage
htmlcov/

# Node / Frontend
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dist/
.cache/
.vite/
.next/
out/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment
.env
.env.local
.env.*.local
back-end/.env
back-end/.env.*
*.log

# Google Cloud
.gcloud/
service-account-key.json
*-key.json

# Blockchain / Contracts
contracts/out/
contracts/cache/
contracts/broadcast/
contracts/lib/
.foundry/

# Databases
*.db
*.sqlite
*.sqlite3

# Secrets
private_key.txt
mnemonic.txt
*.pem
firestore-service-account.json

# Misc
*.bak
*.tmp

# ContextPilot - Project Context (VERSIONED)
# Each project repository has its own .contextpilot/ directory
# This contains project-specific context, history, and documentation
# These files SHOULD be versioned as they are part of the project documentation
# 
# When you do `git init` in a new project, you also create a .contextpilot/ 
# for that project. This context is versioned along with the code.
#
# Structure:
#   .contextpilot/
#     workspaces/
#       {workspace_id}/          # Usually "default" or project name
#         context.md             # Current project context
#         history.json            # Commit history log
#         checkpoint.yaml         # Project state
#         timeline.md             # Project timeline
#         task_history.md         # Full task history
#         milestones.md           # Project milestones
#         retrospectives/        # Retrospective documents
#           *.md
#           *.json

# Ignore workspace directories by default (safety)
.contextpilot/workspaces/*/

# But allow versioning of all context files (they are project documentation)
!.contextpilot/workspaces/.gitkeep
!.contextpilot/workspaces/*/
!.contextpilot/workspaces/*/*.md
!.contextpilot/workspaces/*/*.yaml
!.contextpilot/workspaces/*/*.json
!.contextpilot/workspaces/*/retrospectives/
!.contextpilot/workspaces/*/retrospectives/*.md
!.contextpilot/workspaces/*/retrospectives/*.json

# Ignore temporary/cache files only
.contextpilot/workspaces/*/*.tmp
.contextpilot/workspaces/*/*.bak
.contextpilot/workspaces/*/.cache/

# Ignore user-specific metadata (not part of project context)
.contextpilot/workspaces/*/meta.json

# Keep important config templates
!.env.example
!contracts/.env.example

# Supabase local
**/supabase/.branches/
**/supabase/.temp/

# Lockfiles (keep package-lock but not bun.lockb for clarity)
bun.lockb

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/.terraform.lock.hcl
terraform/budget.tf.bak

# Extension packaging
extension/*.vsix
extension/out/

# Python virtual environments
.venv/
venv/
env/
ENV/

# Archive/temp docs (keep in repo but note as archive)
# docs/archive/ is intentionally tracked for history

